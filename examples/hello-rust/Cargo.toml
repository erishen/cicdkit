[package]
name = "hello-rust"
version = "1.0.0"
edition = "2021"

[[bin]]
name = "hello-rust"
path = "src/main.rs"

# 零依赖示例：仅使用 Rust 标准库（std::net::TcpListener）实现最小 HTTP 服务，
# 用于演示 cicdkit 的构建 / 发布 / 探针闭环。无需任何外部 crate。
[dependencies]
